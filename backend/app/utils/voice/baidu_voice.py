from typing import Dict, Any

# 说明：百度AI语音识别占位。实际应调用官方SDK完成ASR。


def speech_to_text(audio_bytes: bytes, lang: str = "zh") -> Dict[str, Any]:
    # 占位：返回固定文本
    return {"text": "查询订单 12345 修改 状态 为 已发货"}


